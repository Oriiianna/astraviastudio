const fs = require("fs"); let c = fs.readFileSync("src/components/Pricing.css", "utf8"); fs.writeFileSync("src/components/Pricing.css", c.replace("           background-image:", "  background-image:").replace(/\/\* --- Features ---\n   La hairline superior va como background y no como border para poder\n   usar el degradado --grad-hair que ya define el sistema. \*\//, "/* --- Features ---\n   La hairline superior va como background y no como border. */"))
